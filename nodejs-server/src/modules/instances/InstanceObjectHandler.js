export default class InstanceObjectHandler {
  constructor() {
    this.activeInventoryStream = undefined;
    // TODO: Change this to 'containers'
    // Fetch loot with containerId (same as creation code id!)
    this.container = undefined;
  }
}
