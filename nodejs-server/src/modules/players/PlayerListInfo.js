export default class PlayerListInfo {
  constructor(player_name, instance_id, room_index) {
    this.player_name = player_name;
    this.instance_id = instance_id;
    this.room_index = room_index;
  }
}
