function Region(_region_id, _room_index, _owner_client, _local_players, _arrived_patrols) constructor
{
	region_id = _region_id;
	room_index = _room_index;
	owner_client = _owner_client;
	local_players = _local_players;
	arrived_patrols = _arrived_patrols;
}