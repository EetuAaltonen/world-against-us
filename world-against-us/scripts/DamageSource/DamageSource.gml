// TODO: Separate damage types (bullet, melee, elemental, etc.)
function DamageSource(_parent_instance_ref, _bullet, _range, _spawn_point) constructor
{
	parent_instance_ref = _parent_instance_ref;
	bullet = _bullet;
	range = _range;
	spawn_point = _spawn_point;
}