function ContainerInventoryActionInfo(_container_id, _source_grid_index, _target_grid_index, _is_rotated, _is_known) constructor
{
	container_id = _container_id;
	source_grid_index = _source_grid_index;
	target_grid_index = _target_grid_index;
	is_rotated = _is_rotated;
	is_known = _is_known;
}